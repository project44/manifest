---
'@project44-manifest/react': minor
---

DataTable: added `getRowId` prop for stable row ID derivation and added Storybook stories for select
all across pages with manual pagination using an exclusion list pattern
