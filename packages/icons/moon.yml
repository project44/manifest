type: 'library'

workspace:
  inheritedTasks:
    exclude: ['build', 'format', 'lint', 'test', 'typecheck']

tasks:
  build:
    command: 'svgo'
    args:
      - '-f'
      - 'src'
      - '-o'
      - 'lib'
    outputs:
      - 'lib'
