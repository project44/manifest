type: 'library'

tasks:
  build:
    outputs:
      - 'esm'
      - 'lib'

dependsOn:
  - id: 'react-css-baseline'
    scope: 'production'
  - id: 'react-styles'
    scope: 'production'
  - id: 'react-types'
    scope: 'development'
