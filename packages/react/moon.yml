type: 'library'
language: 'typescript'

workspace:
  inheritedTasks:
    rename:
      buildPackage: 'build'

tasks:
  build:
    outputs:
      - 'esm'
      - 'lib'

dependsOn:
  - 'button'
  - 'css-baseline'
  - 'provider'
  - 'styles'
  - 'system'
  - 'tooltip'
