type: 'library'

workspace:
  inheritedTasks:
    rename:
      buildPackage: 'build'

tasks:
  build:
    outputs:
      - 'esm'
      - 'lib'

dependsOn:
  - 'react-button'
  - 'react-styles'
  - 'react-test-utils'
  - 'react-tooltip'
  - 'system'
