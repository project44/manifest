type: 'library'
language: 'typescript'

workspace:
  inheritedTasks:
    rename:
      buildPackage: 'build'

tasks:
  build:
    outputs:
      - 'esm'
      - 'lib'

  props-doc:
    args:
      - '../../../../scripts/propsDoc.mjs'
    options:
      mergeArgs: 'replace'

dependsOn:
  - id: 'button'
    scope: 'development'
  - id: 'styles'
    scope: 'production'
  - id: 'use-merged-ref'
    scope: 'production'
