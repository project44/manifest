type: 'library'
language: 'typescript'

workspace:
  inheritedTasks:
    rename:
      buildPackage: 'build'

tasks:
  build:
    outputs:
      - 'esm'
      - 'lib'

dependsOn:
  - id: 'styles'
    scope: 'production'
  - id: 'types'
    scope: 'development'
  - id: 'typography'
    scope: 'development'
  - id: 'utils'
    scope: 'production'
