type: 'library'
language: 'typescript'

workspace:
  inheritedTasks:
    rename:
      buildPackage: 'build'

tasks:
  build:
    outputs:
      - 'esm'
      - 'lib'

dependsOn:
  - id: 'provider'
    scope: 'production'
  - id: 'transition'
    scope: 'production'
