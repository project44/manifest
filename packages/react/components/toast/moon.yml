type: 'library'
language: 'typescript'

workspace:
  inheritedTasks:
    rename:
      buildPackage: 'build'

tasks:
  build:
    outputs:
      - 'esm'
      - 'lib'

  props-doc:
    args:
      - '../../../../scripts/propsDoc.mjs'
    options:
      mergeArgs: 'replace'

dependsOn:
  - id: 'button'
    scope: 'production'
  - id: 'icons'
    scope: 'production'
  - id: 'styles'
    scope: 'production'
  - id: 'types'
    scope: 'development'
  - id: 'typography'
    scope: 'production'
  - id: 'utils'
    scope: 'production'
