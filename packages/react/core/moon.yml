type: 'library'
language: 'typescript'

workspace:
  inheritedTasks:
    rename:
      buildPackage: 'build'

tasks:
  build:
    outputs:
      - 'esm'
      - 'lib'

dependsOn:
  - 'avatar'
  - 'button'
  - 'css-baseline'
  - 'link'
  - 'provider'
  - 'styles'
  - 'system'
  - 'tooltip'
