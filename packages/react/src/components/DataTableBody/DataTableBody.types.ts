import type { CSS } from '@project44-manifest/react-styles';

export type DataTableBodyElement = 'tbody';

export interface DataTableBodyProps {
  /**
   * Theme aware style object
   */
  css?: CSS;
}
