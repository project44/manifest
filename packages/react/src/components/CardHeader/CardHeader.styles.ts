import { css } from '../../styles';

export const useStyles = css({
  alignItems: 'center',
  display: 'flex',
  justifyContent: 'flex-start',
  width: '100%',
  paddingTop: '$large',
  px: '$large',
  typography: '$title',
});
