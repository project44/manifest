import type { CSS } from '@project44-manifest/react-styles';

export type TooltipContentElement = 'div';

export interface TooltipContentProps {
  /**
   * Theme aware style object
   */
  css?: CSS;
}
