import {
  colorBrandActive,
  colorBrandDefault,
  colorBrandGradient,
  colorBrandHover,
} from '../../constants';

export const brand = {
  active: colorBrandActive,
  default: colorBrandDefault,
  gradient: colorBrandGradient,
  hover: colorBrandHover,
};
