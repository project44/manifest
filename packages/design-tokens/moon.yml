type: 'library'

workspace:
  inheritedTasks:
    exclude: ['buildPackage', 'format', 'format-write', 'lint', 'test', 'typecheck']

tasks:
  build:
    command: 'ts-node'
    args:
      - './scripts/build.ts'
