type: 'library'
language: 'typescript'

workspace:
  inheritedTasks:
    exclude: ['buildPackage']

tasks:
  build:
    command: 'ts-node'
    args:
      - './scripts/build.ts'

dependsOn:
  - 'tokens'
