import { tokens } from '@project44-manifest/design-tokens';

export const fonts = tokens.fontFamily;
export const fontSizes = tokens.fontSize;
export const fontWeights = tokens.fontWeight;
export const letterSpacings = tokens.letterSpacing;
export const lineHeights = tokens.lineHeight;
