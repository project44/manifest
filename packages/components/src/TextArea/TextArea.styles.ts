import { css } from '@project44-manifest/styles';

export const useTextAreaStyles = css({
  '.manifest-textfield-base--input': { py: '$small' },
});

export type { CSS } from '@project44-manifest/styles';
export { cx } from '@project44-manifest/styles';
