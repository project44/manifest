type: 'library'

tasks:
  build:
    outputs:
      - 'esm'
      - 'lib'

dependsOn:
  - id: 'styles'
    scope: 'production'
  - id: 'types'
    scope: 'development'
  - id: 'utils'
    scope: 'production'
