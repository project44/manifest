type: 'application'

workspace:
  inheritedTasks:
    exclude: ['buildPackage']

tasks:
  build:
    command: 'build-storybook'
    args:
      - '-c'
      - '.storybook'
      - '-o'
      - './build'
    deps:
      - '^:build'
    inputs:
      - '@group(storybook)'

  start:
    command: 'start-storybook'
    args:
      - '-p'
      - '6006'
      - '--quiet'
