import { Spinner } from '@project44-manifest/react';
import { withDemoWrapper } from '../../components/DemoWrapper';

function SpinnerMediumExample() {
  return <Spinner size="medium" />;
}

export default withDemoWrapper(SpinnerMediumExample);
