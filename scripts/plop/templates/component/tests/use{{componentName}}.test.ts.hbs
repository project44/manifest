import { renderHook } from '@project44-manifest/test-utils';
import { use{{componentName}} } from '../src';

describe('@project44-manifest/react-{{componentName}} - use{{componentName}}', () => {
  it('should return ...', () => {
    const { result } = renderHook(() => use{{componentName}}({}));

    expect(true).toBeTruthy();
  });
});
