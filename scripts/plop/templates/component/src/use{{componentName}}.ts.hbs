export interface Use{{componentName}}Options {}

export interface Use{{componentName}}Aria {}

export function use{{componentName}}(options: Use{{componentName}}Options): Use{{componentName}}Aria {
  return {}
}
